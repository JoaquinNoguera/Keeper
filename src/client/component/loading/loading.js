import React from 'react'
import './style.scss';

export default function Loading(){
    return(
        <div style={{margin: 'auto'}}>
            <svg>
                <rect/>
            </svg>
        </div>
    );
}